# Plan: Beta slice 1

The rplan artifact referenced by .build-beta-state.json (its existence verifies the rplan step).
Named `*-plan.md` but it does NOT match classify() as a work item? It DOES (token "plan").
