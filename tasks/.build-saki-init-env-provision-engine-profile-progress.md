# build progress — F6 `saki init-env` — provision an engine profile

PRD: `tasks/prd-saki-init-env-provision-engine-profile.md` (Locked · 3 slices)
Branch: `feature/saki-init-env-provision-engine-profile`
Review base: `main` (the pre-existing partial F6 code landed in `c42ef7c`, so slice
reviews are taken against `main` — reviewing against `c42ef7c` would skip it).

## Starting state (recorded 2026-08-18)

The working tree already carried an uncommitted, green partial implementation
(typecheck clean, 376 vitest tests pass, `go vet` clean, Go tests pass), plus an
unrelated backend-daemon feature interleaved in the same files. Snapshotted in
`c42ef7c` to get a clean base. Nothing was thrown away.

Present before this build started:
- `src/commands/init-env.ts` + 5 unit tests, `src/routes.ts` `/api/init-env`,
  `InitEnvResult` type, `saki init-env` registered in `src/index.ts`.
- `backend/usecase/initenv.go` (`InitEnvService`) + 3 tests,
  `backend/infra/initenv.go` (`EngineProvisioner`, fingerprint, scrubbed env),
  `POST /api/init-env` in `backend/adapter/http.go`, wiring in `main.go`.

Known gaps against the PRD acceptance criteria (to close in the loop):
- 1.2/1.3 no real-codex-binary evidence, no repeat-invocation `changed:false` test.
- 1.4 missing-binary exit 1 + "no profile files created" untested.
- 1.5 no adapter test for 403 non-loopback / 422 invalid engine / 422 escaping path
  / adapter-not-invoked.
- `docs/cli-reference.md` has no `init-env` entry (project rule: a route absent from
  the reference is unshipped).
- `NewHandler(..., initEnv ...usecase.InitEnvService)` uses a variadic to dodge a
  call-site change — likely a reviewer finding.

## Slices

- [x] 1 — CLI contract and codex adapter
- [x] 2 — opencode adapter and shared verification
- [ ] 3 — Claude support after F4

## Log

- 2026-08-18 — GATE 1 ok (PRD read, 3 slices). GATE 1.5 ok (`<!-- prd-locked: @codex
  · 2026-08-16 · ui:none -->`). GATE 0 ok (already on the feature branch). GATE 2 —
  STARTING FRESH (no state manifest existed); pre-existing WIP snapshotted to `c42ef7c`.
- 2026-08-20 — F6 slice 2 complete. Added proof-decided Opencode provisioning with fixed
  `opencode plugin @saketek/saki-builder --global` argv, profile-local `XDG_CONFIG_HOME`,
  foreign-engine environment scrubbing, canonical profile locks, symlink-aware relative-profile
  containment, doctor parity, and real-binary Opencode e2e coverage. Focused F6 e2e passed (5 tests),
  direct Vitest passed (383 tests), backend tests passed (680 tests in 5 packages), `go vet` and
  TypeScript passed, and coverage passed at 81.74% statements / 83.51% lines. The full Playwright
  suite remained 7/8 because the unrelated pre-existing `e2e/opencode-spawn.spec.ts` SSE test
  aborted with `write EPIPE`; both F6 init-env specs passed. Focused security disposition: no
  confirmed new HIGH/MEDIUM findings; specialist security agents were unavailable, so no formal
  agent verdict is claimed. Slice 3 remains intentionally unopened.

## Completion

- Code and tests are ready for the slice commit.
- Review boundary: stage only explicit slice-2 paths; do not stage `.claude/skills/mcp-analytics/`
  or the untracked slice-1 context artifacts.
- Full-suite exception is recorded honestly above and in the slice-2 plan; it is unrelated to
  Opencode init-env provisioning.
