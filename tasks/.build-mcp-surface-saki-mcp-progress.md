# Build progress — F3 MCP surface (`saki mcp`)

Branch: `feature/mcp-surface` · PRD: `tasks/prd-mcp-surface-saki-mcp.md` (Locked, SHIP·READY, 2 review rounds)

## Slices
- [x] Slice 1 — MCP server skeleton + `saki_status` — done (rplan-review clean, qa all-pass, reviewer SHIP, security n/a). Commits: 72b1e34 (impl), d95947a (review fixes).
- [x] Slice 2 — Read-only journey tools (doctor, roadmap_list, prd_show, runs) — done (rplan-review clean 9 fixes, qa 14/14, reviewer found+fixed a real prd_show path-traversal bypass on 2nd pass then clean). Commits: 86cbbf2 (impl), 8a7431c (review fixes).
- [x] Slice 3 — Run lifecycle tools (run_start, run_tail, run_stop) — done (3-agent domain-expert pass pre-implementation found+fixed a real build-target path-traversal gap; post-implementation reviewer found+fixed a HIGH — the containment guard was build-only, broadened to every target-taking verb, plus DRY'd into src/mcp/path-guard.ts; mandatory security audit CLEAN, 1 LOW tracked (symlink bypass, needs pre-existing local write access)). Commits: 314d638 (impl), 356fa11 (review fixes).
- [x] Slice 4 — Repo/git + PRD lock tools (branch, branch_list, branch_switch, mr_create, prd_lock) — done (3-agent domain-expert pass pre-implementation independently re-verified the "already mitigated" git-argument-injection reasoning against source, found 0 blockers, fixed 2 test-design gaps; post-implementation reviewer APPROVEd (0 blockers), fixed evidence gaps + DRY anyway; mandatory security audit CLEAN, 0 findings). Commits: a2139ad (impl), da44546 (review fixes). **All 13 tools shipped — F3 v1 scope complete.**

## Open questions
None gate a specific slice (the one §12 open question — run_tail timeout — is explicitly non-blocking).

## Notes
No `/saki-builder:proto` UI handoff — no-UI PRD (headless MCP server), step 3.5 skips every slice.
