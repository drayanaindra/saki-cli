# Project Learned Patterns

> Promoted by /saki-builder:reflect from lessons-learned.md.
> Auto-loaded via the project rules file (CLAUDE.md `@import`).
> Raw notes live in lessons-learned.md — that file is NOT loaded.

Nothing promoted yet. `/saki-builder:reflect` writes confirmed saki-cli-specific patterns here.

Keep this file to patterns that are **true of this repo and not derivable from the code**. Anything
generic and stack-portable belongs in `~/.claude/memory/patterns.md` instead.
